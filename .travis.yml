language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ngravello' ]; then cd .. && eval "mv $currentfolder generator-ngravello" && cd generator-ngravello; fi

